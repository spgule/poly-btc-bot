[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --include=dev", "npm install --prefix server"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server/index.js"
